using System;
using UnityEngine;
using UnityEngine.SceneManagement;

public class GamePlay01Scene : MonoBehaviour
{
    private void Start()
    {
        // SceneManager.LoadScene("GameOverScene", LoadSceneMode.Single);
    }
}
